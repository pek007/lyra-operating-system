# 2026-03-13

- Continuous improvement sweep run at ~03:20 CET.
- Low-risk doc hygiene pass completed: clarified `AGENTS.md` bootstrap-file guidance to match intentionally retained historical artifacts, and moved the `continuous-improvement:sweep` runbook under the correct heading in `CRON_SPEC_AUTONOMOUS_GOVERNANCE_SWEEPS.md`.
- Refreshed OpenClaw release-delta evidence snapshot: `knowledge/evidence/2026-03-13__openclaw-release-delta-snapshot.md`.
- Added placeholder `MEMORY.md` so main-session long-term-memory reads no longer fail on missing file.
- Daily information-model sweep completed at ~09:15 CET. No new external-analysis artifacts required ingest from current workspace inbox/dropzone paths; normalized the already-present report `knowledge/reports/2026-03-12__deepresearch__best-practices-for-an-intent-to-execution-service-feeding-lyra-openclaw-tde__v1.md` with canonical frontmatter so it now indexes correctly; regenerated `knowledge/reports/INDEX.md` (library now indexes 70 report artifacts including the weekly synthesis); reviewed `REGISTRY_SCHEMAS_V1.md` and `DECISION_SCHEMA_V1.md` with no new duplicate terms or schema drift beyond the already-tracked evidence-ingest casing mismatch (`severitySummary`/`linkedTasks` -> `severity_summary`/`linked_tasks`) under `IMP-AUTO-20260307-03`.
- No new maintenance task created: the only action still needed is the already-open `IMP-AUTO-20260307-03` to normalize `tools/evidence_ingest.py` output to canonical snake_case and add regression coverage.
- Major TDE decision-layer buildout completed. Added policy/spec artifacts for decision-to-advancement, workflow-family delegation, runtime embodiment, environment separation, path conventions, staging setup, and runtime promotion/rollback discipline; committed a long sequence of related design/code changes.
- Runtime now supports bounded TDE decision outcomes beyond plain continuation: `continue`, `research_further`, `escalate`, plus re-entry outcomes `retry`, `defer`, and `block`.
- Implemented first real D-layer runtime path in `tools/tde_job_tick_runner.py` and supporting helpers: policy binding validation, decision advancement records, escalation packages, dry-run/no-mutate verification mode, bounded research successor activation, research re-entry, research-loop budget enforcement, and origin-task parking/unparking while research is active.
- Environment professionalization advanced materially: added env-aware runtime paths (`dev`/`staging`/`prod`) for core TDE runtime scripts, cron hook, and several release/evidence scripts; created staging runtime/evidence/config scaffolding; proved staging-only tick execution and native `--env` path resolution.
- First real narrow production-adjacent promotion was executed for the environment/pathing slice, then immediately hardened after verification exposed that `--max-claim 0` was not inert. Added true `--dry-run` no-mutate mode to `tde_job_tick_runner.py` and recorded the promotion + follow-up in `knowledge/evidence/prod/releases/2026-03-13__tde-runtime-pathing-promotion__v1.md`.
- Staging pilots established that the bounded `research_further -> research activation -> re-entry -> continue` loop now works end-to-end after adding the non-claimable parking rule for origin tasks. Evidence recorded in `knowledge/evidence/staging/releases/2026-03-13__tde-decision-loop-staging-pilot__v1.md` and `...__v2.md`.
- Important open issue at compaction time: the first staging retry pilot was contaminated by leftover staging state, so it did not produce a valid retry-path result. Next recommended step is to rerun the retry pilot against an isolated/fresh staging DB fixture rather than the shared staging DB.
- OpenClaw post-update regression observed after upgrade from 2026.3.8 to 2026.3.12: memory search became unavailable because `agents.defaults.memorySearch.provider` was set to `local` while optional peer dependency `node-llama-cpp` was not installed by the update path. Symptom: agent message saying memory search was unavailable due to a local embeddings setup issue. Root cause: local embeddings backend missing after npm global update. Fix applied: `npm i -g node-llama-cpp@3.16.2`, after which `memory_search` succeeded again with local provider. Preventive note: after future OpenClaw updates, explicitly smoke-test memory search when local embeddings are configured.
