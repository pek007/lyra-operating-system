# Staging Cron Notes

Purpose: hold staging-specific wrapper notes and cron invocation shapes for TDE/OpenClaw runtime validation.

Initial target:
- run `tools/tde_job_tick_runner.py` against staging-only DB/objective/binding/evidence paths
- do not reuse production-adjacent defaults
